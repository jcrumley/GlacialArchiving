@model GlacialArchiving.DataAccess.Models.Setting

@{
    ViewBag.Title = "Create Setting";
}

<h2>Create</h2>
<p>
    @Html.ActionLink("Back to List", "Index")
</p>

<div class="tabs">
    <ul>
        <li><a href="#SettingTab">Setting</a></li>
    </ul>
    <div id="SettingTab">
        @using (Html.BeginForm("Create", "Settings", FormMethod.Post, new { @class = "smart-form" }))
        {
            @Html.Partial("_CreateOrEdit", Model)

            <footer>
                <button type="submit" name="submit" class="btn btn-labeled btn-primary">
                    <span class="btn-label"><i class="fa fa-save"></i></span>
                    Create
                </button>
            </footer>
        }
    </div>
</div>

