@model IEnumerable<GlacialArchiving.DataAccess.Models.ValidationError>
@using TBADev.MVC.MVCHelpers

@if (ViewBag.AllowCreate) {
    if (ViewBag.PageDisplayMode == PageDisplayMode.Index) 
    {
<p>
    @Html.ActionLink("Create New", "Create")
</p>
    }
    else if (ViewBag.PageDisplayMode == PageDisplayMode.Inline) 
    {
<p>
    <a href="#" onclick="CreateItem()">Create</a>
</p>        
    }
}
<div style="overflow-x:auto;">
<table cellpadding="0" cellspacing="0" border="0" style="white-space: nowrap;" class="table table-striped table-bordered">
    <thead>
        <tr>
            <th></th>
            <th>UploadedItemValidation</th>            
            <th>Error</th>            
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() > 0)
        {
            foreach (var item in Model)
            {
            <tr>
                <td>
                    @if (ViewBag.PageDisplayMode == PageDisplayMode.Index)
                    {
                        if (ViewBag.AllowEdit) 
                        { 
                            @Html.ActionLink("Edit", "Edit", new { id = item.ValidationErrorId }) 
                        }
                        if (ViewBag.AllowEdit && ViewBag.AllowDelete) 
                        { 
                            @Html.Raw(" | ") 
                        }
                        if (ViewBag.AllowDelete) 
                        {
                            @Html.ActionLink("Delete", "Delete", new { id = item.ValidationErrorId })
                        }
                    }
                    else if (ViewBag.PageDisplayMode == PageDisplayMode.Inline) 
                    { 
                        if (ViewBag.AllowEdit) 
                        { 
                    <a href="#" onclick="EditItem(@item.ValidationErrorId)">Edit</a>
                        }
                        if (ViewBag.AllowEdit && ViewBag.AllowDelete) 
                        { 
                            @Html.Raw(" | ") 
                        }
                        if (ViewBag.AllowDelete)
                        {
                    <a href="#" onclick="DeleteItem(@item.ValidationErrorId)">Delete</a>
                        }
                    }
                </td>

                <td>
                @if (ViewBag.PageDisplayMode == PageDisplayMode.Index) { @Html.ActionLink(item.UploadedItemValidation.Id.ToString(), "Edit", "UploadedItemValidations", new { id = item.UploadedItemValidationId }, null) }
                else { @item.UploadedItemValidation.Id.ToString() }
                </td>
                <td>@if (item.Error != null){ @item.Error } else { @Html.Raw("&nbsp;") }</td>
            </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="100">
                    No records found.
                </td>
            </tr>
        }
    </tbody>
</table>
</div>