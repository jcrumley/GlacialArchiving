@model IEnumerable<GlacialArchiving.DataAccess.Models.Navigation>
@using TBADev.MVC.MVCHelpers

@if (ViewBag.AllowCreate) {
    if (ViewBag.PageDisplayMode == PageDisplayMode.Index) 
    {
<p>
    @Html.ActionLink("Create New", "Create")
</p>
    }
    else if (ViewBag.PageDisplayMode == PageDisplayMode.Inline) 
    {
<p>
    <a href="#" onclick="CreateItem()">Create</a>
</p>        
    }
}
<div style="overflow-x:auto;">
<table cellpadding="0" cellspacing="0" border="0" style="white-space: nowrap;" class="table table-striped table-bordered">
    <thead>
        <tr>
            <th></th>
            <th>Name</th>            
            <th>InConstruction</th>            
            <th>System Generated</th>            
            @if (ViewBag.PageDisplayMode == PageDisplayMode.Index)
            {
            <th>NavItems</th>            
            }
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() > 0)
        {
            foreach (var item in Model)
            {
            <tr>
                <td>
                    @if (ViewBag.PageDisplayMode == PageDisplayMode.Index)
                    {
                        if (ViewBag.AllowEdit) 
                        { 
                            @Html.ActionLink("Edit", "Edit", new { id = item.NavigationId }) 
                        }
                        if (ViewBag.AllowEdit && ViewBag.AllowDelete) 
                        { 
                            @Html.Raw(" | ") 
                        }
                        if (ViewBag.AllowDelete) 
                        {
                            @Html.ActionLink("Delete", "Delete", new { id = item.NavigationId })
                        }
                    }
                    else if (ViewBag.PageDisplayMode == PageDisplayMode.Inline) 
                    { 
                        if (ViewBag.AllowEdit) 
                        { 
                    <a href="#" onclick="EditItem(@item.NavigationId)">Edit</a>
                        }
                        if (ViewBag.AllowEdit && ViewBag.AllowDelete) 
                        { 
                            @Html.Raw(" | ") 
                        }
                        if (ViewBag.AllowDelete)
                        {
                    <a href="#" onclick="DeleteItem(@item.NavigationId)">Delete</a>
                        }
                    }
                </td>

                <td>@if (item.Name != null){ @item.Name } else { @Html.Raw("&nbsp;") }</td>
                <td>@item.InConstruction</td>
                <td>@item.SystemGenerated</td>                
                @if (ViewBag.PageDisplayMode == PageDisplayMode.Index) 
                {
                <td class="rightAligned">@if (item.NavItems != null){ @Html.ActionLink(item.NavItems.Count.ToString(), "TopLevel", "NavItems", new { id = item.Id }, null) } else { @Html.Raw("&nbsp;") }</td>
                }
            </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="100">
                    No records found.
                </td>
            </tr>
        }
    </tbody>
</table>
</div>