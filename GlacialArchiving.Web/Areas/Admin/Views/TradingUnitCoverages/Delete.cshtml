@model GlacialArchiving.DataAccess.Models.TradingUnitCoverage

@{
    ViewBag.Title = "Delete";
    ViewBag.Title = "Delete :: " + @Model.Value;
}

<h2>Delete :: @Model.Value</h2>

<h3>Are you sure you want to delete this?</h3>

<div class="tabs">
    <ul>
        <li><a href="#TradingUnitCoverageTab">TradingUnitCoverage</a></li>
    </ul>
    <div id="TradingUnitCoverageTab">
        @using (Html.BeginForm("Delete", "TradingUnitCoverages", FormMethod.Post, new { @class = "smart-form" }))
        {
            @Html.Partial("_Display", Model)
            
            <footer>
                <a class="btn btn-default" href="../Index">Cancel</a>
                <button type="submit" name="submit" class="btn btn-labeled btn-danger">
                    <span class="btn-label"><i class="fa fa-trash-o"></i></span>
                    Delete
                </button>
            </footer>
        }
    </div>
</div>
