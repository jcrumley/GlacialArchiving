@model GlacialArchiving.DataAccess.Models.NavIcon

@{
    ViewBag.Title = "Create Nav Icon";
}

<h2>Create</h2>
<p>
    @Html.ActionLink("Back to List", "Index")
</p>

<div class="tabs">
    <ul>
        <li><a href="#NavIconTab">NavIcon</a></li>
    </ul>
    <div id="NavIconTab">
        @using (Html.BeginForm("Create", "NavIcons", FormMethod.Post, new { @class = "smart-form" }))
        {
            @Html.Partial("_CreateOrEdit", Model)

            <footer>
                <button type="submit" name="submit" class="btn btn-labeled btn-primary">
                    <span class="btn-label"><i class="fa fa-save"></i></span>
                    Create
                </button>
            </footer>
        }
    </div>
</div>

